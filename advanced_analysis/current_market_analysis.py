#!/usr/bin/env python3
"""
🚀 현재 시장 상황에 대한 거장들의 진짜 생각
"나스닥이 미쳤을 때, 진짜 고수들은 뭘 생각할까?"

2025년 12월 현재: 나스닥 역대급 상승, AI 열풍, 경기 둔화 우려
"""

import yfinance as yf
import pandas as pd
import numpy as np
from datetime import datetime, timedelta

class CurrentMarketWisdom:
    """현재 시장 상황에서 거장들의 진짜 생각을 시뮬레이션"""

    def __init__(self):
        self.current_date = datetime.now()
        self.market_data = self.get_market_snapshot()
        self.context = self.analyze_market_context()

    def get_market_snapshot(self):
        """현재 시장 스냅샷"""
        indices = {
            'nasdaq': yf.Ticker("^IXIC"),
            'snp500': yf.Ticker("^GSPC"),
            'vix': yf.Ticker("^VIX"),
            'dxy': yf.Ticker("DX-Y.NYB")  # 달러 인덱스
        }

        snapshot = {}
        for name, ticker in indices.items():
            hist = ticker.history(period="1y")
            if len(hist) > 0:
                current = hist['Close'].iloc[-1]
                year_ago = hist['Close'].iloc[0]
                ytd_return = (current / hist['Close'].iloc[252] - 1) * 100 if len(hist) > 252 else 0
                snapshot[name] = {
                    'current': current,
                    'year_change': (current / year_ago - 1) * 100,
                    'ytd_change': ytd_return
                }

        return snapshot

    def analyze_market_context(self):
        """현재 시장 컨텍스트 분석"""
        vix_level = self.market_data.get('vix', {}).get('current', 20)
        nasdaq_change = self.market_data.get('nasdaq', {}).get('year_change', 0)

        if vix_level < 15 and nasdaq_change > 30:
            return "euphoric_bull_market"
        elif vix_level > 30 and nasdaq_change < -20:
            return "panic_bear_market"
        elif vix_level < 20 and 15 < nasdaq_change < 30:
            return "optimistic_growth"
        else:
            return "uncertain_transition"

    def warren_buffett_current_thinking(self):
        """워런 버핏이 현재 상황에서 무슨 생각을 할까?"""
        print("👨‍🦳 워런 버핏의 현재 시장 생각")
        print("=" * 60)

        context = self.context
        nasdaq_change = self.market_data.get('nasdaq', {}).get('year_change', 0)

        print(f"📊 현재 시장: 나스닥 {nasdaq_change:.1f}% 상승, 시장 컨텍스트: {context}")

        print(f"\n🧠 버핏의 핵심 철학 적용:")
        print(f"   '시장이 흥분할 때가 가장 조심해야 할 때다'")

        # 버핏의 과거 발언 기반 분석
        if context == "euphoric_bull_market":
            print(f"\n🚨 버핏의 현재 생각:")
            print(f"   \"지금 시장은 1999년 닷컴 버블과 비슷한 느낌이야.")
            print(f"   \"P/E 비율 30을 넘는 기업들이 너무 많아. 이건 지속될 수 없어.\"")
            print(f"   \"나는 지금 현금을 쌓고 있어. 기회를 기다리는 거지.\"")
            print(f"   \"사람들이 AI 주식을 '무한 성장'이라고 부를 때, 나는 조심스러워지지.\"")

            print(f"\n💰 버핏의 현재 액션:")
            print(f"   • 새로운 큰 포지션은 오픈하지 않음")
            print(f"   • 기존 포지션에서 부분 실현 고려 (특히 과열된 성장주)")
            print(f"   • 채권과 현금 비중 증가")
            print(f"   • 가치주가 저렴해질 때까지 기다림")

            print(f"\n🎯 버핏이 말할 법한 현재 코멘트:")
            print(f"   \"오마하의 신사들은 시장의 광기에 흔들리지 않아.")
            print(f"   \"훌륭한 사업이 비싼 가격이면 투자가 아니야.\"")
            print(f"   \"지금은 인내심의 시간이야.\"")

        elif context == "optimistic_growth":
            print(f"\n📈 버핏의 현재 생각:")
            print(f"   \"경제는 괜찮아 보이지만, 가격이 너무 빨리 올랐어.\"")
            print(f"   \"여전히 양질의 기업들이 있지만, 찾기 어려워졌어.\"")
            print(f"   \"선택적 매수만 할 거야.\"")

        print(f"\n🏦 버핏의 Apple 관점 (현재 $273):")
        current_pe = 36.6  # 이전 데이터 기준
        if current_pe > 30:
            print(f"   \"Apple은 여전히 훌륭한 기업이지만, P/E {current_pe}는 비싸.\"")
            print(f"   \"Apple을 더 사려면 최소 20% 하락을 기다려야 해.\"")
            print(f"   \"물론 팔 생각은 없어. 최고의 기업이니까.\"")

    def peter_ynch_current_thinking(self):
        """피터 린치가 현재 상황에서 무슨 생각을 할까?"""
        print(f"\n📈 피터 린치의 현재 시장 생각")
        print("=" * 60)

        context = self.context
        nasdaq_change = self.market_data.get('nasdaq', {}).get('year_change', 0)

        print(f"📊 린치의 핵심 철학: 'Buy what you know, 10-baggers 찾기'")
        print(f"   \"평범한 사람들이 훌륭한 투자자가 될 수 있어\"")

        if context == "euphoric_bull_market":
            print(f"\n🚀 린치의 현재 생각:")
            print(f"   \"1980년대와 비슷한 성장 열풍이야! 하지만 너무 많이 올랐어.\"")
            print(f"   \"AI는 진짜 혁신이지만, 모든 AI 회사가 수익을 내진 않아.\"")
            print(f"   \"사람들이 쓰는 제품을 만드는 회사를 찾아야 해.\"")

            print(f"\n🛍️ 린치의 현재 전략:")
            print(f"   • 쇼핑몰에서 무엇이 팔리는지 관찰 (실제 사용성)")
            print(f"   • 매장에 사람들이 몰리는 기업 찾기")
            print(f"   • 거품이 덜 끼었지만 성장 가능성 있는 중소형주")
            print(f"   • P/E 15-25 사이의 성장주")

            print(f"\n🎯 린치가 좋아할 현재 종목:")
            print(f"   • Apple (사람들이 미쳐서 갈아타는 iPhone)")
            print(f"   • Tesla (실제로 도로에 많아지는 전기차)")
            print(f"   • Amazon (매일 쓰는 e-commerce)")
            print(f"   • 하지만... 52주 최고가 근처라면 조심!")

            print(f"\n⚠️ 린치의 경고:")
            print(f"   \"52주 최고가 근처에서 사는 건 바보 같은 짓이야.\"")
            print(f"   \"올해 50% 올랐다고 내년도 50% 오진 않아.\"")
            print(f"   \"기업의 실제 실적이 따라가야 해.\"")

    def howard_marks_current_thinking(self):
        """하워드 막스가 현재 상황에서 무슨 생각을 할까?"""
        print(f"\n🎢 하워드 막스의 현재 시장 생각")
        print("=" * 60)

        context = self.context
        vix_level = self.market_data.get('vix', {}).get('current', 20)

        print(f"📊 막스의 핵심 철학: 'Market Cycles & Risk Control'")
        print(f"   \"가장 위험한 때가 가장 안전해 보일 때야\"")

        if context == "euphoric_bull_market":
            print(f"\n⚡ 막스의 현재 생각:")
            print(f"   \"지금은 VIX {vix_level}! 완벽한 불안정의 신호야.\"")
            print(f"   \"모두가 수익을 확신할 때가 바로 리스크 관리할 때야.\"")
            print(f"   \"AI 열풍은 2000년 닷컴 버블과 비슷해.\"")

            print(f"\n📉 막스의 현재 전략:")
            print(f"   • 포지션 사이즈 줄이기 (risk management)")
            print(f"   • 더 높은 위험 프리미엄 요구")
            print(f"   • 사이클의 정점에서는 주로 현금 보유")
            print(f"   • 하이일드 채권과 디스트레스드 에셋 관찰")

            print(f"\n🎭 막스가 보내는 메모 (오늘 날짜로):")
            print(f"   \"제목: 현명한 투자자는 기꺼이 사이클을 거부한다\"")
            print(f"   \"지금 시장은 레몬 한 잔에 $10을 내는 것과 같아\"")
            print(f"   \"위험 대비 수익률이 최악이야. 기다려라.\"")

        print(f"\n📈 막스의 미래 예측:")
        print(f"   \"향후 12-18개월 내에 20-30% 조정이 올 거야\"")
        print(f"   \"그때가 바로 진짜 기회가 될 거야\"")
        print(f"   \"지금은 인내심을 보상받을 때야\"")

    def charlie_munger_current_thinking(self):
        """찰리 멍거가 현재 상황에서 무슨 생각을 할까?"""
        print(f"\n🧠 찰리 멍거의 현재 시장 생각")
        print("=" * 60)

        print(f"📊 멍거의 핵심 철학: 'Invert, always invert'")
        print(f"   \"어떻게 망할지 생각해봐라, 그리고 피해라\"")

        print(f"\n🤔 멍거의 현재 생각:")
        print(f"   \"인간의 욕심과 공포는 변하지 않아\"")
        print(f"   \"지금은 '욕심'이 지배하는 시장이야\"")
        print(f"   \"사람들이 '이번에는 다르다'고 말할 때 가장 위험해\"")

        print(f"\n🎯 멍거의 현재 조언:")
        print(f"   • '너무 복잡한 것'은 피해라 (AI 생태계)")
        print(f"   • '이해하기 쉬운' 것에 집중해라 (코카콜라, Apple)")
        print(f"   • '합리적인 가격'은 중요해")
        print(f"   • '시장의 바보'가 되지 마라")

        print(f"\n💬 멍거의 코멘트:")
        print(f"   \"나는 똑똑하지 않아. 그냥 덜 바보일 뿐이야.\"")
        print(f"   \"지금 시장의 바보들이 너무 많아.\"")
        print(f"   \"현명하게 멍청하게 굴어라.\"")

    def ray_dalio_current_thinking(self):
        """레이 달리오가 현재 상황에서 무슨 생각을 할까?"""
        print(f"\n🌍 레이 달리오의 현재 시장 생각")
        print("=" * 60)

        print(f"📊 달리오의 핵심 철학: 'Economic Machine Principles'")
        print(f"   \"모든 것은 사이클이다\"")

        print(f"\n🔄 달리오의 현재 생각:")
        print(f"   \"지금은 장기 부채 사이클의 말미일 수 있어\"")
        print(f"   \"중앙은행들이 금리를 높이면서 경기가 둔화되고 있어\"")
        print(f"   \"자산 가격과 경제 현실이 괴리가 있어\"")

        print(f"\n📈 달리오의 All Seasons Portfolio 조언:")
        print(f"   • 주식 55% (글로벌 분산)")
        print(f"   • 장기 채권 15% (금리 하락 시 대비)")
        print(f"   • 중기 채권 15%")
        print(f"   • 원자재 7.5% (인flation 대비)")
        print(f"   • 금 7.5% (안전자산)")

        print(f"\n⚠️ 달리오의 경고:")
        print(f"   \"모든 자산이 비싸다. 어디에 투자해도 수익률이 낮을 거야\"")
        print(f"   \"현금 보유 기회비용은 높지만, 리스크도 높아\"")
        print(f"   \"분산 투자가 유일한 해답이다\"")

    def generate_current_market_synthesis(self):
        """거장들의 현재 시장에 대한 종합 의견"""
        print(f"\n🎯 거장들의 현재 시장 종합 진단")
        print("=" * 60)

        context = self.context
        nasdaq_change = self.market_data.get('nasdaq', {}).get('year_change', 0)

        print(f"📊 시장 현황: 나스닥 {nasdaq_change:.1f}% 상승, 컨텍스트: {context}")

        consensus = {
            'sentiment': 'cautious_pessimistic',
            'action': 'reduce_positions_or_wait',
            'timeline': '6-18개월 조정 가능성',
            'opportunity': '조정 후 매수',
            'risk_level': 'high'
        }

        print(f"\n🧠 거장들의 합의:")
        print(f"   📉 감성: {consensus['sentiment']} (신중하게 비관적)")
        print(f"   🛑 행동: {consensus['action']} (포지션 축소 또는 관망)")
        print(f"   ⏰ 타임라인: {consensus['timeline']}")
        print(f"   🎯 기회: {consensus['opportunity']}")
        print(f"   ⚠️ 위험 수준: {consensus['risk_level']}")

        print(f"\n💡 개인 투자자를 위한 조언:")
        print(f"   1. 🐰 토끼처럼 빠르게 돈 벌려 하지 마라 (거장들의 공통 조언)")
        print(f"   2. 🐢 거북이처럼 꾸준히, 그리고 신중하게 움직여라")
        print(f"   3. 💰 현금 비중을 20-30%까지 늘려라 (기회를 위해)")
        print(f"   4. 📈 과열된 성장주는 부분 실현하라")
        print(f"   5. 🏪 실제 돈을 버리는 비즈니스에 집중하라")

        print(f"\n🎭 현실 직시:")
        print(f"   \"지금 시장이 붕괴한다는 말이 아니야\"")
        print(f"   \"다만, 투자하기에는 위험이 커졌다는 뜻이야\"")
        print(f"   \"현명한 투자자는 위험을 관리할 줄 안다\"")
        print(f"   \"지금은 배우고 기다릴 시간이다\"")

        return consensus

def main():
    """메인 함수 - 현재 시장 상황 분석"""
    print("🌪️ 2025년 12월 현재 시장: 거장들의 진짜 생각")
    print("=" * 80)

    wisdom = CurrentMarketWisdom()

    # 각 거장의 현재 생각
    wisdom.warren_buffett_current_thinking()
    wisdom.peter_ynch_current_thinking()
    wisdom.howard_marks_current_thinking()
    wisdom.charlie_munger_current_thinking()
    wisdom.ray_dalio_current_thinking()

    # 종합 의견
    wisdom.generate_current_market_synthesis()

    print(f"\n🎯 최종 요약:")
    print(f"\"나스닥이 미쳐도, 거장들은 절대 미치지 않는다\"")
    print(f"\"지금은 신중해야 할 때다. 돈 잃고 싶지 않으면...\"")
    print(f"\"기회는 반드시 온다. 준비된 자만 잡을 수 있을 뿐.\"")

if __name__ == "__main__":
    main()